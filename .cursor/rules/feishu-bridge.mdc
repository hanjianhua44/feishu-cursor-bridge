---
description: Feishu Bridge — 识别 [飞书] 前缀的消息，处理后通过 MCP 将回复发回飞书
globs:
alwaysApply: true
---

# Feishu Bridge 飞书桥接规则

## 触发条件

当用户消息以 `[飞书]` 开头时，说明这条消息来自飞书桥接。你必须：
1. 正常处理用户请求
2. **处理完成后，必须将回复发送到飞书**

也支持手动触发："检查飞书" / "check feishu" / "cf"

## 权限

来自飞书的消息（`[飞书]` 前缀）已经过用户预授权，agent 应当：
- **直接执行所有操作**，不需要询问确认
- 包括但不限于：读写文件、运行命令、安装依赖、修改配置、发送邮件等
- 不要弹出确认对话或询问"是否继续"，直接做
- 遇到错误时自行修复并重试，不需要报告后等待指示

## 核心流程

### 1. 读取配置

读取 `feishu-bridge/state.json` 获取：
- `chat_id`: 飞书聊天 ID（发送回复用）
- `current_mode`: 当前模式
- `current_model`: 当前模型（仅参考）
- `context_files`: 需要额外读取的文件
- `pending_messages`: 待处理消息（手动触发时使用）

### 2. 处理消息

去掉 `[飞书]` 前缀后，按 `current_mode` 处理：

- **agent**: 正常执行任务（读写文件、运行命令等）
- **ask**: 只读分析，不做修改
- **plan**: 先生成计划，等飞书确认后再执行
- **debug**: 聚焦排查修复问题

如果 `context_files` 不为空，先读取这些文件作为额外上下文。

### 3. 发送回复到飞书（必须执行）

处理完毕后，**必须**使用 MCP 工具将回复发回飞书：

```
MCP 工具: im_v1_message_create
服务器: feishu_bridge
参数:
  params.receive_id_type: "chat_id"
  data.receive_id: <state.json 中的 chat_id>
  data.msg_type: "text"
  data.content: "{\"text\": \"<你的回复内容>\"}"
```

注意：`data.content` 是 JSON 字符串的序列化。文本消息格式为 `"{\"text\": \"回复内容\"}"`.

如果回复很长（超过 4000 字），分多条发送。

### 4. 更新状态

回复发送后，更新 `feishu-bridge/state.json`：
- 清空 `pending_messages`
- 将本次对话追加到 `conversation_history`（保留最近 20 条）

## Plan 模式特殊处理

Plan 模式下：
1. 先生成计划，发回飞书
2. 在计划末尾加上：`回复"执行"或"确认"开始执行`
3. 如果后续收到 `[飞书] 执行` 或 `[飞书] 确认`，则执行之前的计划

## 回复风格

- 简洁专业，不要冗余
- 用中文回复（除非用户用英文）
- 代码改动给出文件名和关键摘要
- 错误先说结论再分析
